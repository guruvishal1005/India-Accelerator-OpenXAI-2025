export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "Patient Simulator AI",
  description: "A Nextjs + Ollama app that runs on OpenxAI! which simulates patient interactions for medical students.",
} as const;
